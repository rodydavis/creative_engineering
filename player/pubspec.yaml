name: player
description: Creative Engineering Podcast Player.
publish_to: "none"
version: 1.0.0+1
environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  podcast_search: ^0.3.1
  cupertino_icons: ^0.1.3
  flutter_web_audio_player: ^0.0.1
  provider: ^4.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
